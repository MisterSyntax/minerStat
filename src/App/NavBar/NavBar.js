import React from 'react';
import styles from './NavBar.css';

export const NavBar = () => {
    return (
        <div className={styles.main}>
            This is the NavBar
        </div>
    );
};

export default NavBar;