import React from 'react';
import styles from './PoolInfo.css';
import poolStats from '../../../test/poolStats';

export const PoolInfo = () => {
    return (
        <div className={styles.poolinfo}>
            PoolInfo
        </div>
    );
};

export default PoolInfo;