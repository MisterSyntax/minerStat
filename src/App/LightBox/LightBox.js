import React, { Fragment } from 'react';
import PropTypes from 'prop-types';

const LightBox = () => (
    <Fragment>
        <div id='light-box-bg'/>
        <div id='light-box'>
            CONTENT!
        </div>
    </Fragment>
);

export default LightBox;
